# The smallest useful Dockerfile:
# 1. Start from an existing image (someone else already built Alpine Linux).
# 2. Tell Docker what to run when a container starts from it.
FROM alpine:3.20

CMD ["echo", "Hello, Docker world!"]
